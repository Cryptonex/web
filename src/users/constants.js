import login from './login/constants';
import registration from './registration/constants';
import activation from './activation/constants'
import profile from './profile/constants';
import settings from './settings/constants';
import restorePassword from './restorePassword/constants';

export default Object.assign({

}, login, registration, activation, profile, settings, restorePassword);